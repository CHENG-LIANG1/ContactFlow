export type AppLanguage = "zh" | "en";

export type AccentId = "paper" | "sage" | "sky" | "amber";

export type UserProfile = {
  avatarUri?: string;
  name: string;
  bio: string;
  email: string;
};
